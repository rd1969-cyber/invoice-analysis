# Streamlit Community Cloud reads this (repo root) to build the app environment.
# Runtime deps for ui/streamlit_app.py only. The FastAPI/DB/test deps live in
# backend/requirements.txt and are NOT needed to run the hosted app.
#
# Pinned to the exact versions verified working locally so the cloud build
# reproduces that environment (avoids "works on my machine, blank on the cloud").
streamlit==1.58.0
pandas==3.0.3        # exact local version (45 tests + AppTest verified on this)
pdfplumber==0.11.9
openpyxl==3.1.5
xlrd==2.0.2
